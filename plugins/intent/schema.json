{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "fill": {
      "type": "string",
      "enum": ["heuristic", "off"],
      "default": "heuristic",
      "description": "How to handle history tool calls whose intent was never captured."
    }
  }
}
