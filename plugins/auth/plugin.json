{
  "schema_version": 1,
  "id": "torana/auth",
  "name": "auth",
  "version": "0.1.0",
  "abi_version": "v2",
  "minimum_torana_version": "0.1.0",
  "failure_mode": "pass",
  "repository": "https://github.com/torana-edge/torana-plugins",
  "description": "Enterprise Gateway auth normalization. NOT published to the public plugin registry and not a general-purpose authentication plugin \u2014 it is a reference for the capability surface only. Do not deploy it as an access control.",
  "hooks": [
    {
      "name": "run_before_request"
    }
  ],
  "permissions": [
    {
      "name": "env.host_call.verify_virtual_key",
      "description": "verify torana virtual keys"
    },
    {
      "name": "env.request_headers",
      "description": "read allowlisted auth headers (Authorization, X-Api-Key, X-Torana-*)"
    },
    {
      "name": "env.set_identity",
      "description": "Identity injection for virtual keys"
    },
    {
      "name": "env.block_request",
      "description": "Reject virtual keys explicitly denied by the authoritative verifier"
    }
  ]
}
