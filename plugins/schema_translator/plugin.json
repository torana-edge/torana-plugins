{
  "id": "torana/schema_translator",
  "name": "schema_translator",
  "version": "0.3.0",
  "abi_version": "v1",
  "minimum_torana_version": "0.1.0",
  "failure_mode": "pass",
  "repository": "https://github.com/torana-edge/torana-plugins",
  "description": "KV-array schema translation: converts additionalProperties maps to arrays and reverses on response",
  "hooks": [
    {"name": "run_before_request", "priority": 100},
    {"name": "run_on_stream_chunk", "priority": 100}
  ],
  "permissions": [
    {"name": "env.meta_get", "description": "Read mutation registry and fragment buffers"},
    {"name": "env.meta_set", "description": "Write mutation registry and fragment buffers"},
    {"name": "env.log", "description": "Diagnostic logging"}
  ]
}
