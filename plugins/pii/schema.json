{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "tools": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [
        "*"
      ],
      "description": "Tool-name allowlist; * scans every tool result."
    },
    "on_error": {
      "type": "string",
      "enum": [
        "block",
        "allow"
      ],
      "default": "block",
      "description": "Fail closed or open if the contextual scanner cannot decide."
    },
    "max_scan_bytes": {
      "type": "integer",
      "minimum": 0,
      "default": 0,
      "description": "Maximum tool-output BYTES sent to the operator-bound scanner service (rune-safe); zero is unbounded. Content beyond the limit is an incomplete scan governed by on_error and is never cached as clean."
    }
  }
}
