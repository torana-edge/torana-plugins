{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "provider": {"type": "string", "description": "Configured local provider used for contextual PII scanning."},
    "model": {"type": "string", "description": "Local model used for PII scanning."},
    "api_key_env": {"type": "string", "description": "Optional environment-variable name for the configured provider."},
    "tools": {"type": "array", "items": {"type": "string"}, "default": ["*"], "description": "Tool-name allowlist; * scans every tool result."},
    "on_error": {"type": "string", "enum": ["block", "allow"], "default": "block", "description": "Fail closed or open if the contextual scanner cannot decide."},
    "max_scan_chars": {"type": "integer", "minimum": 0, "default": 0, "description": "Maximum tool-output characters to send to the local scanner; zero is unbounded."}
  }
}
